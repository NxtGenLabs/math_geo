import 'package:flutter/cupertino.dart';

class AdvancedLevel extends StatelessWidget {
  const AdvancedLevel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
